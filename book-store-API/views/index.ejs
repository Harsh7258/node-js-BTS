<h2 class="page-header">Recently Added</h2>
<div class="recent-flex">
    <% books.forEach(book => { %>
      <a href="/books/<%= book.id %>">
        <img height="150" width="130" src="<%= book.coverImagePath %>">
        <p><%= book.title %></p>
      </a>
    <% }) %>
</div>
